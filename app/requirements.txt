# Core dependencies
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6

# Moondream local inference (Photon engine)
moondream>=1.3.0
# kestrel 0.4.2 (latest PyPI release) predates moondream3.1; pin the upstream
# commit that adds moondream3.1 while still matching the published
# kestrel-kernels==0.4.7 wheels (later commits need unreleased kernels).
# Drop once kestrel >0.4.2 ships.
kestrel @ git+https://github.com/m87-labs/kestrel@fdcd0a530b80cdb083aea1373e97a4b0e38ce7cb
# kestrel's native extensions ship per-torch-minor builds (2.9-2.12 as of now)
torch>=2.9,<2.13
pillow>=9.0.0

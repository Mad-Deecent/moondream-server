1. Port forward to access the service:
   kubectl port-forward --namespace {{ .Release.Namespace }} svc/{{ include "moonstream-server.fullname" . }} 8080:{{ .Values.service.port }}

2. Test the API:
   curl http://localhost:8080/health

3. Run the test suite:
   python app/test_api.py

{{- if .Values.persistence.modelCache.enabled }}
4. Check model download status:
   kubectl get jobs --namespace {{ .Release.Namespace }} -l "app.kubernetes.io/component=model-download"
{{- end }}
